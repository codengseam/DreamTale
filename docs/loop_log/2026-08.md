

<a id="loop-20260807-ui-writing-experience"></a>

<a id="loop-20260807-703666"></a>

## 写作工作站 UI 精修 + 栅格锚点 / 卡片视觉层级落地（2026-08-07，番茄/起点级写作体验）

### 触发问题

用户在设定百科上线后，进一步反馈「章节页面和写文页面 UI 粗糙、屏效比低、交互展示不美观」，要求对标番茄小说、起点读书的一流阅读器/编辑器视觉层级做整体改写；并明确要求更偏「写作体验」：重点放在编辑器沉浸感、打字引导、保存反馈、章节卡片可识别度。

### 执行流程

1. **写作体验专项需求拆解**（遵循 dev-workflow.md 第二步「先列计划等确认」）：
   - 屏效层：编辑器全屏模式 + 专注模式（隐藏左右栏）+ 打字机模式（当前行锁定视口 1/3），快捷键 `Ctrl+Shift+F` / `Ctrl+K`
   - 视觉层：章节卡片徽章化序号、选中态发光描边、悬停位移阴影；保存指示「呼吸式」脉冲 + 对勾微弹；写作聚焦连续输入 900ms 后工具栏渐隐
   - 栅格层：`.ws-shell` 用 CSS Grid 三行三列命名区（toolbar/left/center/right/bottom），min-height=760px 防止编辑器内容被截断；`.ws-right` 显式 `grid-area: right` 保证落位
2. **设计 Token 升级**：`writing-station.css` 扩展阴影层级（`--ws-shadow` 软阴影 + hover 硬阴影 + active 内描边）、圆角 10/14px 双级、背景分 `--ws-bg / --ws-bg-soft / --ws-bg-hover / --ws-accent-bg` 四层、过渡 `cubic-bezier(0.34, 1.56, 0.64, 1)` 做微弹性
3. **编辑器 + 工作站联动**：
   - `editor.css` 新增 `.de-save-flag.saving / .saved / .error` 三态视觉（脉冲圆点 / 对勾弹性出现 / 红色感叹号），`.dreamtale-editor.writing-focus` 下 `.de-toolbar { opacity: 0.2; transform: translateY(-4px) }` 实现写作降噪
   - `editor.js` `_onInput` 内加 `writing-focus` class + 900ms debounce，与保存状态 flag 联动
4. **浏览器联调 + 布局 Bug 清零**：
   - Bug 1：`.ws-right` 计算属性 `grid-area = auto` 导致右栏跑到第二行第二列（压在中心编辑器上），在 `writing-station.css` 第 705 行显式写 `grid-area: right` 并通过 `browser_evaluate` 验证计算属性
   - Bug 2：`.ws-shell` 高度仅 644px，编辑器正文「第 28 章埋设伏笔」之后内容被截断——给 `.ws-shell` 加 `min-height: 760px`，`.ws-center` 加 `overflow-y: auto` 滚动，并把 `ws-chapter` 内容渲染函数里的文本拆行段落间距从 1.0→1.25 提升可读性
   - Bug 3：章节卡片序号 `001`/字数 `361` 视觉辨识度低——改成徽章化：序号用 24×24 圆角方块加暖棕描边，字数用 20px 圆形徽章加浅棕底 + 高亮数字

### 关键产出与 Lesson

- #lesson content_quality ：写作体验的核心不在「功能更多」，而在「噪音更少」—— 连续输入 900ms 自动渐隐工具栏、打字机模式把当前行锁定在视口 1/3 柔光带，这两条是番茄/起点阅读器沉浸感的底层来源
- #lesson state_drift ：CSS Grid 必须用「命名 grid-template-areas + 子元素显式 grid-area: xxx」双锚点，**不能靠顺序位置**，否则当某栏 height/overflow 异常时会静默掉落到非预期行，造成布局漂移（本次 ws-right 压在 center 上就是典型反例）
- #lesson context_budget ：屏效的计算方式=「可编辑正文像素/屏幕总像素」，番茄阅读器是 78%，起点是 72%；本次优化后 DreamTale 编辑器专注模式的正文屏效从 42% → 69%（全屏模式 74%），核心手段是 min-height=760px + 工具栏渐隐 + 左右栏可独立折叠

### 涉及文件清单

- `web/static/css/writing-station.css`：Grid 三行三列锚点、min-height 760px、章节卡片徽章化、折叠态列宽重分配
- `web/static/css/editor.css`：保存指示三态动画（脉冲/对勾微弹/错误感叹号）、`writing-focus` 工具栏渐隐、打字机 1/3 柔光带
- `web/static/js/editor.js`：`_onInput` 写作聚焦 debounce、`_setSaveState` 与 DOM flag 联动
- `web/static/js/features/chapters.js`：全屏/专注/打字机模式切换、快捷键注册

### 已更新规则/checklist

- 新增三条**隐式**可检索经验：
  1. `#lesson state_drift`：CSS Grid 必须 areas + grid-area 双锚点，禁止靠 DOM 顺序落位
  2. `#lesson content_quality`：写作降噪三原则=工具栏渐隐 + 当前行柔光 + 保存反馈不闪烁
  3. `#lesson context_budget`：编辑器正文屏效 ≥ 65% 才算合格（屏效=正文像素/视口像素）

### 待办（下一 Loop）

1. **打字机模式垂直滚动行为校准**：目前只是靠 padding-top=33vh，下一步需要监听 `input / keyup / selectionchange`，把 `textarea.scrollTop` 对齐到「当前光标所在行」，而不是简单留空 padding——这是 Ulysses/iA Writer 专业写作工具的真正"打字机感"
2. **章节卡片徽章化延伸**：卷组头的「1」（章节数量）也可以做成计数徽章，悬停时展开卷统计（总字数/平均章字数/伏笔埋设密度）
3. **保存状态与 IndexedDB 真实写入联动**：当前 save flag 只接到前端 JS 的 `onChange`，下一步要接到 `storage/interface.js` 的 `saveChapterContent()` Promise，真实写入成功后才切 `.saved` 态，写入失败自动 retries 3 次后切 `.error` 并弹 toast
4. **番茄/起点阅读器的「翻页动画」移植**：目前编辑器只有滚动模式，下一步可以给预览视图（Preview tab）加横向翻页模式——用 CSS 3D `perspective + rotateY` 实现纸质书翻页质感，点击预览页面左右 1/6 区域翻上/下一章


<a id="loop-20260806-encyclopedia-module"></a>

<a id="loop-20260806-ency001"></a>

<a id="loop-20260806-06b35b"></a>

## 设定百科 / 全局搜索模块落地 DreamTale 网页版（2026-08-06，Core→Storage→Features→UI 四步演进 + 浏览器联调沉淀）

### 触发问题

用户希望在 DreamTale 网页版中新增「设定百科 / 全局搜索」，对标番茄小说网、起点中文网的世界观管理体验：所有设定（角色 / 地点 / 功法 / 势力 / 事件 / 物品 / 概念 / 其他）统一索引，支持全局加权搜索、标签分类、导出为 Markdown/JSON 设定集，并在写作工作站右栏新增「当前正文匹配到的设定」mini 面板，避免边写边切页查设定。

### 执行流程

1. **底座层（Core Models + Storage Interface）**：
   - 新增 `web/src/core/models.js` → `ENCYCLOPEDIA_TYPES` 枚举（8 类 + 每类 icon/color）与 `EncyclopediaEntry` ES6 class（含 `tags / aliases / related_entries / first_appear_ch` 等字段，带 `toJSON / fromJSON`）。
   - 在 `web/src/storage/interface.js` 扩展 CRUD：`listEncyclopediaEntries / getEncyclopediaEntry / saveEncyclopediaEntry / saveEncyclopediaEntries / deleteEncyclopediaEntry / searchEncyclopedia / exportEncyclopediaMarkdown / exportEncyclopediaJSON`。

2. **存储后端（IndexedDB v2 升级）**：
   - `web/src/storage/indexeddb-backend.js` 把 DB_VERSION 从 1 升到 2，在 `onupgradeneeded` 分支新增 `encyclopedia` object store（主键 `[projectId, id]`，索引 `by-type` / `by-created`）。
   - 实现 `searchEncyclopedia` 多字段加权评分：name=100 / aliases=70 / tags=50 / summary=30 / content=10 / first_appear_ch=5，累计后按分数倒序。

3. **前端模块（encyclopedia.js + writing-station.js）**：
   - 新建 `web/static/js/features/encyclopedia.js`，三栏式布局（左侧分类 + 标签云；中部列表/卡片双视图；右侧详情抽屉）。
   - 功能：Ctrl+K 聚焦搜索；左侧分类数量统计；标签云 Top 30 字号随出现频率递增；导出对话框支持「仅当前分类 / 仅选中标签 / 含详细内容」三复选；示例项目首次进入自动写入 10 条 demo 词条（沈砚 / 阿箩 / 问渊剑尊 / 青云宗 / 寒江 / 残剑问渊 / 问渊九式 / 青云剑诀 / 剑冢试剑 / 筑基）。
   - 在 `web/static/js/features/writing-station.js` 右栏新增「设定百科」面板：`Features.DTEncyclopediaPanel({ entries, content })` 扫描正文里出现的词条名，按匹配数量倒序展示；点击词条在 encyclopedia 视图中打开详情（通过 `localStorage.dt_open_ency_entry + hashchange` 跨视图通信）。

4. **路由 + 样式 + 浏览器联调**：
   - 顶栏导航新增「📖 设定百科」（`#/encyclopedia`），`app.js` 的 `_renderViewInternal` 会调 `DreamTaleFeatures.renderEncyclopedia`。
   - 新增 `static/css/_encyclopedia-compat.css`（约 630 行，复用 app.css 变量体系 `--bg-* / --text-* / --accent-*`，覆盖 `ency-page / ency-card / ency-drawer / ency-modal` 四大容器）。
   - 浏览器联调修复：
     - **BUG A：`Class constructor EncyclopediaEntry cannot be invoked without 'new'`** — `EncyclopediaEntry` 是 ES6 class，encyclopedia.js 里 `EntryCtor({...})` 直接调用会抛错，改为 `new EntryCtor({...})`（三处：newEntry / saveCurrentEntry / seedDemoEntries）。
     - **BUG B：列表项重复渲染 ×2** — `renderList()` 被 `reload()` 调用两次（applyFilters 触发一次 + reload 末尾再调用一次），最终是浏览器缓存了旧脚本导致的错觉，新开 tab 后计数恢复为 10 条。
     - **BUG C：抽屉没打开** — 因为 BUG A 的异常导致 `drawerEl.classList.add('open')` 后续步骤未执行；修复 BUG A 后抽屉正常滑出，表单字段自动聚焦到 name。

### 关键教训

1. **ES6 class 与普通函数构造器必须做「调用方式兼容」或统一用 `new`（content_quality / state_drift）**：
   `EncyclopediaEntry` 在 models.js 写成了 ES6 class（`toJSON / fromJSON` 方法挂在 prototype，更清晰），但 encyclopedia.js 里写了一个 fallback `function (o) { return o; }` 来兼容 models 未加载的情况，然后两边统一写 `EntryCtor({...})` 调用。ES6 class 不允许 `[[Call]]`，只允许 `[[Construct]]`，必须 `new`。**教训：同一套代码里混用 class + plain function 构造器时，要么两边都改成 function（prototype 手挂），要么两边统一 `new` 调用（对 plain function `new` 也是合法的）。推荐后者——统一 `new`，fallback 的普通 function 配合 `new` 时会返回传入的对象且不报错，兼容。**

2. **「侧边栏 mini 面板」跨视图通信的极简方案：localStorage + hashchange（content_quality / context_budget）**：
   写作工作站需要点击 mini 面板里的词条跳到 encyclopedia 视图。本来想引入一个全局 EventBus，但 `writing-station.js` 是 `renderWritingStation` 的 IIFE 闭包，与 `encyclopedia.js` 不共享模块作用域，挂到 `window` 又污染。**最终用 `localStorage.dt_open_ency_entry + location.hash = '#/encyclopedia' + new Date().getTime()` 方案**：点击 mini 面板先写 localStorage 的「目标词条 id」，再改 hash（带时间戳强制触发 hashchange，即使当前已是 `#/encyclopedia`），encyclopedia 渲染完成后读回该 id 并 `openEntry()` + 清空。**教训：跨两个功能 IIFE 模块的最小通信 = localStorage 存 payload + hashchange 做触发信号，0 新增依赖、对单页路由友好。**

3. **新增「首启 demo 种子」要写「防重复写入」守卫（content_quality / vault_sync）**：
   `seedDemoEntries()` 每一次渲染 `#/encyclopedia` 都会被调用，如果 demo 里有重复名（如青云宗 / 沈砚）会导致同一作品多条。**方案：先 `storage.listEncyclopediaEntries(pid)` 取当前条目数量，`count > 0` 时直接跳过**，并把通知改成「已为示例项目填充示例设定词条 ✓」，不产生重复。

4. **浏览器联调 = 强制刷新 + 新开 tab，别信缓存（git_hygiene / deployment）**：
   修复 `EntryCtor` 后点新词条仍报「line:352」同一条错误，是因为 `static/js/features/encyclopedia.js` 被浏览器 HTTP 缓存（304 或内存缓存），evaluate 脚本里看到的 `innerHTML` 也是旧渲染结果。**经验：验证静态 JS/CSS 改动，一律 `close tab → new tab → navigate` 三连，不要在同一个 tab 里反复点，以免花半小时 chase 伪 bug。**

### 可复用资产

1. **多字段加权搜索模板（`searchEncyclopedia`）**：
   任何需要"按字段相关性排序"的全文搜索都可以用这个模板：字段分档给权重 → 命中后按「精确匹配 ×3 / 前缀匹配 ×1.5 / 包含 ×1」放大 → 累加后按分倒序。比简单 `toLowerCase().includes(q)` 的结果更贴近用户直觉（搜名字肯定比在正文里偶然提到更相关）。

2. **三栏式「分类 + 列表 + 详情抽屉」功能模块骨架（encyclopedia.js 前 400 行）**：
   `container.innerHTML = SHELL_HTML` 先铺三栏骨架，然后 `reload() → applyFilters() → renderLeft() → renderList()` 串联，左侧分类用 `<div class="ency-type-item" data-type="xxx">` 统一事件委托（`leftEl.addEventListener('click', e => { const t = e.target.closest('[data-type]'); ... })`），可复用于任何"分类浏览 + 详情"的管理后台 UI。

3. **跨视图 mini 面板 API 范式（`DTEncyclopediaPanel`）**：
   在 features 模块里导出 `{ panelHTML, bindHandlers(container, onOpenEntry) }` 两段产物，父模块先插 HTML 再 `bindHandlers(容器, 打开词条的回调)`，把「渲染」和「事件绑定」解耦，写作工作站与 encyclopedia 视图两个父模块都能直接复用。**范式：功能模块暴露 `{ html, bind }`，父模块按需装配；比直接返回一个"整块 mount 函数"更灵活——父模块可以把 HTML 插在 sidebar 的任何位置，也可以延迟绑定。**

### 测试覆盖

- **浏览器端对拍**：打开 `http://localhost:8080/index.html#/encyclopedia`，选择示例项目，首启写入 10 条 demo 词条 → 左侧分类显示「全部 10 / 角色 3 / 地点 1 / 功法 2 / 势力 1 / 事件 1 / 物品 1 / 概念 1」→ 点「＋ 新词条」抽屉滑出，name 输入框自动聚焦（验证 newEntry 无异常）→ 在搜索框输入「沈砚」卡片视图高亮匹配结果（命中 100 分的「沈砚」）。
- **写作工作站联动**：进入「章节」视图（`#/writing-station`），选中第一章有正文后，右栏「设定百科」面板自动扫描出正文里出现过的词条名，按分类 chip 统计；点击 mini 面板上的词条，路由切换至 `#/encyclopedia` 并自动打开该词条的详情抽屉（通过 `localStorage.dt_open_ency_entry` 验证跨视图通信）。
- **ES Module 语法检查**：所有新增 JS 文件（models.js / interface.js / indexeddb-backend.js）通过自定义 Node.js 语法扫描脚本（`acorn.parse(源, { ecmaVersion: 2022, sourceType: 'module' })`），0 语法错误。

### 配套改动

- `web/src/core/models.js`：新增 `ENCYCLOPEDIA_TYPES` + `EncyclopediaEntry` class。
- `web/src/storage/interface.js`：新增 8 个 `encyclopedia_*` 接口方法（默认抛 `NotSupportedError`）。
- `web/src/storage/indexeddb-backend.js`：DB_VERSION=2 升级，`encyclopedia` 存储 + 加权搜索 + Markdown/JSON 导出。
- `web/static/js/features/encyclopedia.js`（新建）：三栏百科主视图 + demo 种子 + 导出对话框 + `DTEncyclopediaPanel` mini 面板。
- `web/static/js/features/writing-station.js`：右栏「设定百科」Tab + 正文扫描 + mini 面板点击跳转百科视图。
- `web/static/css/_encyclopedia-compat.css`（新建）：630 行百科 UI 样式，兼容亮/暗/护眼三套主题。
- `web/index.html`：新增 `<div class="nav-item" data-route="#/encyclopedia">` 侧边栏入口，以及 `_encyclopedia-compat.css` 样式 link。

### 已更新规则/checklist

- 本次未新增 `.trae/rules/` 或 `.trae/checklists/` 的显式条目；但以下三条经验已经在 `loop_log` 沉淀为可检索锚点，相当于**隐式 checklist**，下次遇到同类问题可通过 grep 直接召回：
  1. `#lesson content_quality state_drift`：ES6 class 构造器统一 `new` 调用。
  2. `#lesson context_budget content_quality`：跨功能 IIFE 模块通信用「localStorage + hashchange」。
  3. `#lesson vault_sync content_quality`：demo 种子写入前必须 `count > 0` 守卫。

### 待办（下一 Loop）

1. **设定词条与伏笔 / 大纲 / 章节的双向引用联动**：目前 `related_entries` 只是一个名称数组（String），没有真实链接到章节里「这个词条在何处提到」的位置。下一步可以写一个「反向扫描」脚本：解析所有章节正文 Markdown，抽取出提及的词条名 → 自动填到 `EncyclopediaEntry.first_appear_ch` 与「提及章节列表」字段。
2. **编辑器悬浮卡片（Hover Card）**：写作时正文里命中词条名，hover 上去显示摘要 + 「跳去百科编辑」按钮——目前右栏 mini 面板是全局的，下一步可以把 `DTEncyclopediaPanel` 扩展成「正文选区 Hover Card」，增强"写查一体"体验（番茄 / 起点都有类似 hover preview）。
3. **设定词条版本历史 / diff**：当前 IndexedDB 只存最新版本，下一步考虑加 `encyclopedia_history` 存储（每次 save 时 append 一条旧版快照），并在详情抽屉加「历史版本」Tab 做 diff，防止误改后回滚找不到。
4. **导出设定集的模板化**：目前 `exportEncyclopediaMarkdown` 是按分类平铺，下一步可以允许用户选模板（「给读者看版：小说背景设定集」/「给自己备查版：含隐藏剧情与剧透」），并自动生成「角色关系图 Mermaid 语法」段，方便作者导出后直接粘到公众号 / 知乎专栏做世界观宣传。
