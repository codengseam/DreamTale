{
  "name": "dreamtale-web",
  "version": "0.1.0",
  "description": "DreamTale Web 端：Core 层数据模型 + 存储引擎（IndexedDB / File System Access API / ZIP 导入导出）",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
